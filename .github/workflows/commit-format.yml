# Commit format check
# Calls the org reusable gate, so the logic lives in one place and
# this repository runs exactly one execution per pull request.
name: Commit format

on:
  pull_request:

permissions:
  contents: read

jobs:
  check:
    uses: Obsidian-Solutions/.github/.github/workflows/commit-format.yml@f8db4ab90072c80a0e9abb3a8ac3e288823070fc
