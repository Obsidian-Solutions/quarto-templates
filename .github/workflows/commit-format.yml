# Commit format check
# Calls the org reusable gate, so the logic lives in one place and
# this repository runs exactly one execution per pull request.
name: Commit format

on:
  pull_request:

permissions:
  contents: read

jobs:
  check:
    uses: Obsidian-Solutions/.github/.github/workflows/commit-format.yml@9d338ad3b4c9c29b6de12ef5da78f7337b12c501
