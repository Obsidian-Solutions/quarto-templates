name: Scorecard

on:
  push:
    branches: [main]
  schedule:
    - cron: "0 0 * * 0"
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  id-token: write

jobs:
  scorecard:
    uses: Obsidian-Solutions/.github/.github/workflows/scorecard.yml@af4cf710b0dffe76495fabe1329793de481fcbd3
