# Commit format check
# Calls the org reusable gate, so the logic lives in one place and
# this repository runs exactly one execution per pull request.
name: Commit format

on:
  pull_request:

permissions:
  contents: read

jobs:
  check:
    uses: Obsidian-Solutions/.github/.github/workflows/commit-format.yml@fde4d54499dba0322c068ca89599f2acdded5de3
