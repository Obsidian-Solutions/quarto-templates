name: Dependabot auto-merge

on:
  pull_request_target:
    types: [opened, synchronize, reopened, labeled]

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    uses: Obsidian-Solutions/.github/.github/workflows/dependabot-auto-merge.yml@af4cf710b0dffe76495fabe1329793de481fcbd3
