% SPDX-License-Identifier: MIT
$if(toc)$
$if(toc-title)$
\renewcommand*\contentsname{$toc-title$}
$endif$
{
$if(colorlinks)$
$if(toccolor)$
\hypersetup{linkcolor=$toccolor$}
$endif$
$endif$
\setcounter{tocdepth}{$toc-depth$}
\tableofcontents
}
$endif$
$if(lof)$
$if(has-figures)$
\clearpage
\listoffigures
$endif$
$endif$
$if(lot)$
$if(has-tables)$
\clearpage
\listoftables
$endif$
$endif$
% Format documents (letter, memo, invoice, agenda) have no front
% matter: titlepage-none starts arabic pagination from page 1 in
% before-body.tex, so the body flows straight after the letterhead
% or masthead. Reports only switch from roman to arabic here, after
% the lists, so every front-matter page carries a roman number.
$if(titlepage-none)$
$else$
\clearpage
\pagenumbering{arabic}
$endif$
